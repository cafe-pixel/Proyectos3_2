using UnityEngine;

public class Assets : MonoBehaviour,IGiveDamage
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void Damage()
    {
        //soltar un item
    }
}
